{
  "name": "@code-sync-bridge/integration-tests",
  "version": "1.0.0",
  "description": "Integration test workspace for end-to-end sync flows.",
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "jest --watch --runInBand",
    "test:coverage": "jest --coverage --runInBand",
    "clean": "node ../scripts/clean-paths.js coverage dist"
  },
  "dependencies": {
    "@code-sync-bridge/shared": "^1.0.0",
    "@code-sync-bridge/intranet-client": "^1.0.0",
    "@code-sync-bridge/extranet-client": "^1.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0",
    "ssh2": "^1.15.0",
    "simple-git": "^3.19.0",
    "tmp": "^0.2.1",
    "@types/tmp": "^0.2.3"
  }
}
